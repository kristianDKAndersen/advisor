<!-- Usage: fill this in once per session, after a worker's final `result` (or the Advisor's own Step 8 report), and write it to that run's $outputDir/RESULT.md. -->

## Completed
<!-- What shipped, with paths cited (body.paths from the result envelope, or files touched). -->

## Verification
<!-- How the --goal verifier was actually checked: pass|fail, plus the evidence (test output, claim-to-evidence table, evaluator scores.json). -->

## Remaining Work
<!-- "none", or a list of named gaps (e.g. from a partial/blocked verdict or an evaluator's overall_pass: false). -->
