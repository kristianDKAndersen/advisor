{
  "name": "example",
  "steps": [
    {
      "agent": "researcher",
      "task_template": "Research the assigned topic. Context from prior step: {{prev_summary}}",
      "goal_template": "deliver findings"
    }
  ]
}
