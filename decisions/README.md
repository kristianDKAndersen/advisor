# decisions/

Archived decision records and evaluation memos, relocated out of the repo root
(2026-07-07 doc/prune audit). These document *why* shipped features were built
the way they were; they are historical evidence, not live specs, and nothing in
the codebase references them programmatically.

- `mcp-sqlite-vault-dbhub-decision.md` — selection of bytebase/dbhub as the
  read-only SQLite MCP server for the vault (was a stray `changes.md` at root).
- `measurement-output-filter.md` — corrected token-measurement memo for the
  `lib/output-filter.js` feature.
- `h5i-token-reduction-eval.md` — technology evaluation memo (conditional-go verdict).
- `advisor-landing-draft.md` — marketing/overview landing-page copy draft
  (the built page lives in the design-staging area, outside version control).
